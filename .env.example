# Set these in the process environment. The CLI does not automatically read .env.
OPENAI_API_KEY=
# Choose a model available to your account with Responses + Structured Outputs support.
OPENAI_MODEL=
